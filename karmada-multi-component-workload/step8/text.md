Now, you will create a Volcano Job. This custom resource contains multiple nested components, each requiring different resources.

The job manifest `volcano-job.yaml` was pre-created for you.

Command to run:

RUN `kubectl --kubeconfig /etc/karmada/karmada-apiserver.config apply -f volcano-job.yaml`{{exec}}
