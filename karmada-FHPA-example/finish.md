**Summary**

In this scenario, you deployed a multi-cluster nginx workload and used FederatedHPA to scale it automatically based on CPU usage.

You also configured multi-cluster service routing with ServiceExport and ServiceImport, then verified scale up under load and scale down after traffic stopped.
